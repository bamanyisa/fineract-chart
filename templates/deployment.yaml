apiVersion: apps/v1
kind: Deployment
metadata:
  name: fineract-server
  labels:
    app: fineract-server
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount | default 1 }}
  {{- end }}
  selector:
    matchLabels:
      app: fineract-server
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: fineract-server
    spec:
      initContainers:
        - name: init-db
          image: busybox:1.28
          command:
            - sh
            - -c
            - |
              echo "Checking for PostgreSQL..."
              while ! nc -z {{ .Values.database.host }} {{ .Values.database.port }}; do
                sleep 1
              done
      containers:
        - name: fineract-server
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
              name: http
          env:
            - name: FINERACT_NODE_ID
              value: "{{ .Values.fineractEnv.FINERACT_NODE_ID }}"
            - name: FINERACT_HIKARI_DRIVER_SOURCE_CLASS_NAME
              value: "{{ .Values.fineractEnv.FINERACT_HIKARI_DRIVER_SOURCE_CLASS_NAME }}"
            - name: FINERACT_HIKARI_JDBC_URL
              value: "{{ .Values.fineractEnv.FINERACT_HIKARI_JDBC_URL }}"
            - name: FINERACT_HIKARI_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.secretName }}
                  key: {{ .Values.fineractEnv.FINERACT_HIKARI_USERNAME_KEY }}
            - name: FINERACT_HIKARI_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.secretName }}
                  key: {{ .Values.fineractEnv.FINERACT_HIKARI_PASSWORD_KEY }}
            - name: FINERACT_DEFAULT_TENANTDB_HOSTNAME
              value: "{{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_HOSTNAME }}"
            - name: FINERACT_DEFAULT_TENANTDB_PORT
              value: "{{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_PORT }}"
            - name: FINERACT_DEFAULT_TENANTDB_UID
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.secretName }}
                  key: {{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_UID_KEY }}
            - name: FINERACT_DEFAULT_TENANTDB_PWD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.secretName }}
                  key: {{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_PWD_KEY }}
            - name: FINERACT_DEFAULT_TENANTDB_CONN_PARAMS
              value: "{{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_CONN_PARAMS }}"
            - name: FINERACT_DEFAULT_TENANTDB_TIMEZONE
              value: "{{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_TIMEZONE }}"
            - name: FINERACT_DEFAULT_TENANTDB_IDENTIFIER
              value: "{{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_IDENTIFIER }}"
            - name: FINERACT_DEFAULT_TENANTDB_NAME
              value: "{{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_NAME }}"
            - name: FINERACT_DEFAULT_TENANTDB_DESCRIPTION
              value: "{{ .Values.fineractEnv.FINERACT_DEFAULT_TENANTDB_DESCRIPTION }}"
            - name: FINERACT_SERVER_SSL_ENABLED
              value: "{{ .Values.fineractEnv.FINERACT_SERVER_SSL_ENABLED }}"
            - name: FINERACT_LOGGING_LEVEL
              value: "{{ .Values.fineractEnv.FINERACT_LOGGING_LEVEL }}"
            - name: JAVA_TOOL_OPTIONS
              value: "{{ .Values.fineractEnv.JAVA_TOOL_OPTIONS }}"
          resources:
            limits:
              cpu: {{ .Values.resources.limits.cpu | quote }}
              memory: {{ .Values.resources.limits.memory | quote }}
            requests:
              cpu: {{ .Values.resources.requests.cpu | quote }}
              memory: {{ .Values.resources.requests.memory | quote }}
          livenessProbe:
            httpGet:
              path: /fineract-provider/actuator/health
              port: http
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /fineract-provider/actuator/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
